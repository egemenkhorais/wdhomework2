HW2 Implementation Report
Implemented Features

Navigation & Consistency: Updated the global navigation menu across all pages (index.html, about.html, projects.html, contact.html) to include the new HW2 pages: Overview, Business Card, Login, and Chatbot.

CSS Fundamentals (Specificity & Selectors):

Added specificity.html and specificity.txt to demonstrate CSS specificity calculations based on Lab 4 exercises.

Applied advanced selectors (e.g., :nth-child for table rows) and refined typography/spacing throughout the site.

Demonstrated CSS placement: External (style.css), Internal (inside overview.html), and Inline (inside chatbot.html or login.html).

Business Card Page: Created card.html and card.css featuring a profile image, specialization (3rd-year student), email link, and GitHub link.

Responsive Layout (Overview):

Developed overview.html using CSS Grid for the main page structure and Flexbox for the skills/cards sections.

Implemented a Media Query to ensure a single-column layout on mobile devices (max-width: 768px).

Included 4 project cards (MetroYonetim, Smart Dustbin, Company Management, Uğurlu Bal), with the "Uğurlu Bal" card styled differently to satisfy the requirement.

UI Design (Figma to CSS):

Designed a two-panel Authentication interface in Figma.

Exported design tokens and CSS into figma-auth.css.

Interactivity (JavaScript Chatbot):

Created chatbot.html, chatbot.css, and chatbot.js.

The chatbot handles dynamic DOM updates, processing user input through an array of objects (question bank) and appending responses to the chat window.

Validation & Publishing: All HTML and CSS files have been validated. The project is successfully published on GitHub Pages.

Known Limitations

The chatbot uses a static predefined question bank and does not feature real-time AI processing.

The login form is a front-end demonstration only; it does not currently handle backend authentication or database storage.

Figma CSS export was manually refined from "Absolute Positioning" to "Flexbox" to improve responsiveness across different screen resolutions.
